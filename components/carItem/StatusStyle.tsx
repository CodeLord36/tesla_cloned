import { StyleSheet } from "react-native";

const StatusStyle = StyleSheet.create({
  // your styles here
  status: {
    alignItems: "center",
  },

  statusText: {
    color: "white",
    fontSize: 20,
    fontWeight: "bold",
  }
});

export default StatusStyle;
